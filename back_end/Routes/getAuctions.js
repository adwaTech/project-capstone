module.exports = (req,res)=>{
    // owner posted auctions
    // all, posted, won, lost
    // all auctions except owner's
    // nearby auctions, popular, category, specific, latest
    res.send(req.body);
}