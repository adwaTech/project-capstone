import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class YourBids extends StatelessWidget{
  Widget build(BuildContext context)=>Scaffold(
    appBar: AppBar(
      title: Text('Your bids'),
    ),
      body: Center(child: Text('hi'),),
    );
}